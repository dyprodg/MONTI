# Development Dockerfile with Air for hot reload
FROM golang:1.23-alpine

# Set working directory
WORKDIR /app

# Install Air for hot reload and git
# Use v1.49.0 (last version before namespace change)
RUN apk add --no-cache git && \
    go install github.com/cosmtrek/air@v1.49.0

# Copy go mod files
COPY go.mod go.sum ./

# Download dependencies
RUN go mod download

# Source code will be mounted as a volume
# This allows for hot reloading during development

# Expose port
EXPOSE 8080

# Run Air for hot reload
CMD ["air", "-c", ".air.toml"]
