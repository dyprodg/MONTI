name: Infrastructure CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - 'Infra/**'
      - '.github/workflows/infra.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'Infra/**'
      - '.github/workflows/infra.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: Infra

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Placeholder validation
        run: |
          echo "Infrastructure validation placeholder"
          echo "Infra code will be validated here when implemented"
          ls -la || true
